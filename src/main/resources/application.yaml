spring:
  session:
    store-type: redis
    timeout: 2592000
  application:
    name: ZeroStack-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ZeroStack
    username: root
    password: waxyf2022
  profiles:
    active: local
  data:
    redis:
      host: localhost
      port: 6379
      password:
      ttl: 3600

server:
  port: 8080
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.ZeroStack.controller
knife4j:
  enable: true
  setting:
    language: zh_cn

